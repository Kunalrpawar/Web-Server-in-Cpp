#include <stdio.h>

#include "TestServer.hpp"

int main(){
    HDE::TestServer t;  // Create an instance of TestServer
    // The server will start listening for connections immediately upon creation
    return 0;  // Exit the program
}